# Development

Install and verify:

```bash
npm install
npm run build
npm test
npm run lint
```

Run:

```bash
npm run dev -- --config config.yaml
```

Custom config:

```bash
npm run dev -- --config ./my-config.yaml
```

Commit convention:

```text
feat(scope): summary

Scope, verification, and notes.
```
