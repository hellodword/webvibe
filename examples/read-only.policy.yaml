version: 1
mode: read-only
workspace:
  root: "${workspaceRoot}"
  protected: []
upstreams: {}
tools:
  - name: relay.info
    type: builtIn
limits:
  maxToolOutputBytes: 60000
  timeoutMs: 30000
audit:
  enabled: true
