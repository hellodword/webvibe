version: 1
mode: dev
extends: ../policies/read-only.yaml
upstreams:
  task-runner:
    transport: stdio
    command: docker
    args:
      - run
      - --rm
      - -i
      - -v
      - "${workspaceRoot}:/workspace"
      - example-task-mcp:latest
tools: []
